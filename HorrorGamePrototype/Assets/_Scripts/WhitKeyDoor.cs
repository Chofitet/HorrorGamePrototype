using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhitKeyDoor : MonoBehaviour
{
    DragginSystem dragginSystem;
    // Start is called before the first frame update
    void Start()
    {
        dragginSystem = GetComponent<DragginSystem>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
